export class Card {
    constructor(id, title, content, positionX, positionY, width, height, canvasId, createdAt) {
      this.id = id;
      this.title = title;
      this.content = content;
      this.positionX = positionX;
      this.positionY = positionY;
      this.width = width;
      this.height = height;
      this.canvasId = canvasId;
      this.createdAt = createdAt;
    }
  }
  